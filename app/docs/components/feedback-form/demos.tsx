"use client"

import { FeedbackForm } from "@/components/ui/feedback-form"

/** Sentiment rating that reveals an optional comment once a face is picked. */
export function FeedbackFormDemo() {
  return <FeedbackForm />
}
