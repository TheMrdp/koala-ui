"use client"

import { ContactForm } from "@/components/ui/contact-form"

/** Full contact block: name + email row, subject Select, message with live count. */
export function ContactFormDemo() {
  return <ContactForm />
}
