"use client"

import { WaitlistForm } from "@/components/ui/waitlist-form"

/** Centered waitlist capture with a social-proof avatar stack and running count. */
export function WaitlistFormDemo() {
  return <WaitlistForm count={1284} />
}
